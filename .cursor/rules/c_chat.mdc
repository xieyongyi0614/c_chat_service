---
alwaysApply: true
---

---

title: "NestJS IM 聊天室 - Cursor 规则"
description: "用于 NestJS IM 聊天室项目的 Cursor AI 编码规则和约定"
author: "你的名字或团队名"
version: "1.0.0"
tags: ["nestjs", "im", "chat", "prisma", "mysql", "cursor"]

---

# 🧠 核心模块设计原则

## 认证与授权 (`auth`)

- 使用 JWT (JSON Web Tokens) 进行用户认证。
- 实现基于角色的访问控制 (RBAC) 或声明式访问控制 (如 `@UseGuards(JwtAuthGuard)` )。
- 密码必须经过哈希处理（推荐使用 bcrypt）后再存储。

## 用户管理 (`user`)

- 提供用户注册、登录、信息获取、更新等基础功能。
- 用户状态（在线/离线）可通过 WebSocket 连接状态或心跳机制维护。

## 聊天会话 (`chat`)

- 区分私聊 (一对一) 和群聊 (多对多)。
- 聊天会话实体应包含参与者列表、创建时间、最后消息时间等元数据。
- 实现会话列表查询接口。

## 消息 (`message`)

- 消息类型支持文本、图片、文件等（通过枚举或联合类型定义）。
- 每条消息关联发送者和所属聊天会话。
- 实现消息历史记录的分页查询。
- 考虑消息已读/未读状态的标记与同步。
